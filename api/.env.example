DB_HOST=localhost
DB_USER=theartis_lavapi
DB_PASSWORD=your_password_here
DB_NAME=theartis_lavpris
JWT_SECRET=change_this_to_a_random_string
PORT=3000
CORS_ORIGIN=https://lavprishjemmeside.dk

# Email configuration (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM_NAME=Lavprishjemmeside.dk
EMAIL_FROM_ADDRESS=info@lavprishjemmeside.dk

# Password reset configuration
PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=60
PASSWORD_RESET_BASE_URL=https://lavprishjemmeside.dk

# AI: Anthropic API for page content generation
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# AI: Base URL for internal API calls (e.g. fetching /ai/context). Default: http://localhost:3000
# API_BASE_URL=http://localhost:3000

# Pexels API (media automation)
PEXELS_API_KEY=
PEXELS_DEFAULT_LOCALE=da-DK
PEXELS_MAX_PER_GENERATION=6
PEXELS_MAX_FILE_SIZE_MB=15

# Publish: GitHub PAT for triggering deploy workflow (workflow_dispatch)
# Create at: GitHub → Settings → Developer settings → Personal access tokens
# Required scopes: repo (or workflow)
GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx
# Repo for publish trigger (owner/repo). Default: kimjeppesen01/lavprishjemmeside.dk
# GITHUB_REPO=owner/repo-name

# Google Search Console + GA4 (Traffic Dashboard at /admin/traffic/)
# Setup: https://console.cloud.google.com → create service account → enable:
#   - Google Search Console API (searchconsole.googleapis.com)
#   - Google Analytics Data API (analyticsdata.googleapis.com)
# Then grant the service account email:
#   - Search Console: Settings → Users and permissions → Add user (Restricted)
#   - GA4: Admin → Property access management → Add user (Viewer)
GOOGLE_SERVICE_ACCOUNT_EMAIL=lavpris-traffic-reader@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"
# GOOGLE_SITE_URL: must match exactly what is verified in Search Console.
#   Domain property (recommended): sc-domain:lavprishjemmeside.dk
#   URL prefix property:          https://lavprishjemmeside.dk/
GOOGLE_SITE_URL=sc-domain:lavprishjemmeside.dk
# GA4 Property ID: 9-digit numeric ID found in GA4 → Admin → Property Settings
# (NOT the G-XXXXXXX measurement ID)
GOOGLE_GA4_PROPERTY_ID=123456789

# Master dashboard — IAN agent heartbeat key (no JWT needed for IAN Python agent)
# Generate: openssl rand -hex 32
MASTER_API_KEY=
