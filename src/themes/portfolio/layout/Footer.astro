---
// src/themes/portfolio/layout/Footer.astro
import headerFooter from '../../../data/header-footer.json';

const siteName = headerFooter.footer_site_name || 'Studio';
const navLinks = Array.isArray(headerFooter.footer_menu_1) ? headerFooter.footer_menu_1 : [];
const email = headerFooter.footer_email || '';
---
<footer class="bg-[var(--color-bg-dark)] text-[var(--color-text-on-dark)] py-16">
  <div class="mx-auto px-[var(--container-padding-x)]" style="max-width: var(--container-max-width)">
    <div class="flex flex-col md:flex-row justify-between gap-10 mb-12">
      <div class="max-w-xs">
        <p class="text-xl font-bold mb-3" style="font-family: var(--font-heading)">{siteName}</p>
        {email && <p class="text-sm text-white/60"><a href={`mailto:${email}`} class="hover:text-white transition-colors">{email}</a></p>}
      </div>
      {navLinks.length > 0 && (
        <nav class="flex flex-wrap gap-6 items-start">
          {navLinks.map((link: { href: string; label: string }) => (
            <a href={link.href} class="text-sm text-white/50 hover:text-white transition-colors">{link.label}</a>
          ))}
        </nav>
      )}
    </div>
    <p class="text-xs text-white/30">&copy; {new Date().getFullYear()} {siteName}</p>
  </div>
</footer>
